services:
  api:
    environment:
      AI_MODE: ${AI_MODE:-fixture}
      DATABASE_REQUIRED: "true"
      MOENV_API_KEY: ""
      CWA_API_KEY: ""
      ALLOWED_ORIGINS: http://localhost:8080
    ports:
      - "3000:3000"

  web:
    ports:
      - "8080:80"
